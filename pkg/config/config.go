package config

// Every X seconds, the bot will check to see if it's time to send any scheduled messages
const LoopWaitSeconds = 10

// Every Y seconds, print successes/failures to stdout
const HeartbeatSeconds = 60
